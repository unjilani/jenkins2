pipeline {
    agent any

    stages {
        stage('Execute shell script') {
            steps {
               script{
                sh './scripting.sh'
               }
            }
        }
    }
}